import { expectType } from 'tsd';
import { todo } from '../src/index.js';

// Type-level tests using tsd
// These tests run at compile time to ensure type safety

// Example: expectType<string>(todo());
